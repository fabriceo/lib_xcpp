
#include <platform.h>
#include <stdio.h>

extern void testcmdline();

int main(int argc, char * argv[]) {

    testcmdline();

    for (int x = 0; x < argc; x++)
		printf("Arg %d %s\n", x, argv[x]);
	return argc;

    return 0;
};
